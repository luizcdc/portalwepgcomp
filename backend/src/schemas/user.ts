export interface Student {
    name: string
    surname?: string
    registration: string
    email: string
    advisor: string
    photo: string
    program: string
}

export interface Teacher {
    name: string
    surname?: string
    registration: string
    email: string
}