export interface PresentationData {
  id?: string;
  titulo: string;
  doutorando: string;
  emailDoutorando?: string;
  orientador?: string;
  date?: string;
  local?: string;
  time?: string;
  descricao?: string;
}